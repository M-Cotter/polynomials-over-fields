# polynomials-over-fields
Implementations of basic algorithms for polynomials over fields. Including the extended euclidean algorithm, repeated squares and polynomial division. Additionally, implementations of some basic fields.
